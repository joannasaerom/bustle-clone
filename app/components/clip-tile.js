import Ember from 'ember';

export default Ember.Component.extend({
  // findClipArticles: function(articles){
  //   articles.forEach(function(article) {
  //     var output: [];
  //     if(article.isClip) {
  //       output.push(article);
  //     }
  //     return output;
  //   })
  // }
});
