
import { preview } from 'bustle-clone/helpers/preview';
import { module, test } from 'qunit';

module('Unit | Helper | preview');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = preview([42]);
  assert.ok(result);
});

