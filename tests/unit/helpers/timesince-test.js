
import { timesince } from 'bustle-clone/helpers/timesince';
import { module, test } from 'qunit';

module('Unit | Helper | timesince');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timesince([42]);
  assert.ok(result);
});

